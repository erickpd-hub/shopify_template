{{ 'section-hero.css' | asset_url | stylesheet_tag }}

<section class="hero" style="
  {% if section.settings.background_image != blank %}
    background-image: url('{{ section.settings.background_image | image_url: width: 1920 }}');
  {% endif %}
">
  <div class="hero__overlay"></div>
  
  <div class="container">
    <div class="hero__content">
      {% if section.settings.show_badge %}
        <div class="hero__badge">
          <svg class="hero__badge-icon" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor">
            <path d="M12 2L15.09 8.26L22 9.27L17 14.14L18.18 21.02L12 17.77L5.82 21.02L7 14.14L2 9.27L8.91 8.26L12 2Z" />
          </svg>
          <span>{{ section.settings.badge_text }}</span>
        </div>
      {% endif %}

      <h1 class="hero__title">{{ section.settings.title }}</h1>
      
      {% if section.settings.description != blank %}
        <p class="hero__description">{{ section.settings.description }}</p>
      {% endif %}

      <div class="hero__buttons">
        {% if section.settings.button_primary_text != blank %}
          <a 
            href="{{ section.settings.button_primary_link }}" 
            class="hero__button hero__button--primary"
          >
            {{ section.settings.button_primary_text }}
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor">
              <path d="M5 12h14M12 5l7 7-7 7"/>
            </svg>
          </a>
        {% endif %}

        {% if section.settings.button_secondary_text != blank %}
          <a 
            href="{{ section.settings.button_secondary_link }}" 
            class="hero__button hero__button--secondary"
          >
            {{ section.settings.button_secondary_text }}
          </a>
        {% endif %}
      </div>
    </div>
  </div>
</section>

<style>
  .hero {
    position: relative;
    min-height: 90vh;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 8rem 1rem 5rem;
    background-size: cover;
    background-position: center;
    background-repeat: no-repeat;
  }

  .hero::before {
    content: '';
    position: absolute;
    inset: 0;
    background: linear-gradient(to bottom, #0f172a, #1e293b, #0f172a);
    z-index: 0;
  }

  .hero__overlay {
    position: absolute;
    inset: 0;
    background-image: inherit;
    background-size: cover;
    background-position: center;
    opacity: 0.2;
    z-index: 1;
  }

  .hero .container {
    position: relative;
    z-index: 10;
  }

  .hero__content {
    max-width: 56rem;
    margin: 0 auto;
    text-align: center;
  }

  .hero__badge {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.5rem 1rem;
    border-radius: 9999px;
    backdrop-filter: blur(12px);
    -webkit-backdrop-filter: blur(12px);
    background: rgba(251, 191, 36, 0.1);
    border: 1px solid rgba(251, 191, 36, 0.2);
    color: #fbbf24;
    margin-bottom: 1.5rem;
  }

  .hero__badge-icon {
    width: 16px;
    height: 16px;
  }

  .hero__title {
    color: white;
    font-size: clamp(2.5rem, 5vw, 4rem);
    font-weight: 600;
    margin-bottom: 1.5rem;
    line-height: 1.2;
  }

  .hero__description {
    color: rgba(255, 255, 255, 0.7);
    font-size: 1.25rem;
    line-height: 1.6;
    margin-bottom: 2.5rem;
    max-width: 42rem;
    margin-left: auto;
    margin-right: auto;
  }

  .hero__buttons {
    display: flex;
    flex-direction: column;
    gap: 1rem;
    justify-content: center;
    align-items: center;
  }

  @media (min-width: 640px) {
    .hero__buttons {
      flex-direction: row;
    }
  }

  .hero__button {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.75rem 2rem;
    border-radius: 0.5rem;
    font-weight: 500;
    text-decoration: none;
    transition: all 0.2s;
    font-size: 1rem;
  }

  .hero__button--primary {
    background: #fbbf24;
    color: #0f172a;
    box-shadow: 0 10px 15px -3px rgba(251, 191, 36, 0.2);
  }

  .hero__button--primary:hover {
    background: #f59e0b;
    transform: translateY(-2px);
    box-shadow: 0 20px 25px -5px rgba(251, 191, 36, 0.3);
  }

  .hero__button--secondary {
    backdrop-filter: blur(12px);
    -webkit-backdrop-filter: blur(12px);
    background: rgba(255, 255, 255, 0.05);
    border: 1px solid rgba(251, 191, 36, 0.3);
    color: white;
  }

  .hero__button--secondary:hover {
    background: rgba(255, 255, 255, 0.1);
  }

  .hero__button svg {
    width: 20px;
    height: 20px;
  }
</style>

{% schema %}
{
  "name": "Hero Section",
  "settings": [
    {
      "type": "image_picker",
      "id": "background_image",
      "label": "Background Image"
    },
    {
      "type": "checkbox",
      "id": "show_badge",
      "label": "Show Badge",
      "default": true
    },
    {
      "type": "text",
      "id": "badge_text",
      "label": "Badge Text",
      "default": "Colección 2025"
    },
    {
      "type": "text",
      "id": "title",
      "label": "Title",
      "default": "Fragancias para Cada Estación"
    },
    {
      "type": "textarea",
      "id": "description",
      "label": "Description",
      "default": "Descubre nuestra exclusiva colección organizada por las cuatro estaciones del año. Encuentra tu fragancia perfecta para cada momento."
    },
    {
      "type": "text",
      "id": "button_primary_text",
      "label": "Primary Button Text",
      "default": "Explorar Colecciones"
    },
    {
      "type": "url",
      "id": "button_primary_link",
      "label": "Primary Button Link",
      "default": "/collections"
    },
    {
      "type": "text",
      "id": "button_secondary_text",
      "label": "Secondary Button Text",
      "default": "Ver Ofertas"
    },
    {
      "type": "url",
      "id": "button_secondary_link",
      "label": "Secondary Button Link",
      "default": "/collections/all"
    }
  ],
  "presets": [
    {
      "name": "Hero Section"
    }
  ]
}
{% endschema %}
